ex) 회원가입 내용을 디비에 저장하고 싶다.

클라이언트
통신을 할 경로를 서버와 맞추고
회원가입을 하기 위해 입력한 값들을 객체 형식으로 서버에 전달한다

서버
서버는 디비에 create를 통해 테이블안에 데이터를 넣는다.
넣고 나면 res.send를 통해 console.log 보내도됨(디비에 저장했다)

클라이언트에서 alert을 통해 회원가입에 성공했습니다 띄워주면 됨


클라이언트 -> 데이터를 정해진 위치로 보냄 -> 필요한 정보를 서버에서 다시 보냄  (res.send) findOne,all where ->
그 받은 정보를 어떻게 처리할지(변수) -> 

검색 해보면 좋을거 : .data.userId  // [0]


